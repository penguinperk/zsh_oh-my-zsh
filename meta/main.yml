---
galaxy_info:
  author: Scott Perkins
  description: Validates that zsh is installed and installs oh_my_zsh with a default of my configuration.
  company: Personal
  license: "license (BSD, MIT)"
  min_ansible_version: 2.5
  platforms:
    - name: MacOSX
      versions:
        - all
        - any
  galaxy_tags:
    - development
    - system
    - packaging

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.